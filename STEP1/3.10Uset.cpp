#include <bits/stdc++.h>
using namespace std;

void explainUnorderdSet(){
    unordered_set<int> st;
    /*lower_bound and upper_bound functio
    does not works, rest all function are same as set
    it doesnot store elements in any order
    it has a better complexity than set in most cases,except some when collision happens
    */
}